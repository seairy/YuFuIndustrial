<div class="container header">
  <div class="row">
    <div class="col-xs-12 mb20 center-block">
      <%= image_tag @page.poster, class: 'img-responsive center-block poster' %>
    </div>
    <% @branches.each do |branch| %>
    <div class="col-xs-3">
      <div class="branch">
        <div class="logo">
          <%= link_to image_tag(branch.logo, class: 'img-responsive'), branch %>
        </div>
        <div class="content">
          <h3><%= link_to branch.short_name(I18n.locale), branch %></h3>
          <div class="description">
            <%= branch.short_description(I18n.locale).truncate(32) %>
          </div>
        </div>
        <div class="clear"></div>
      </div>
    </div>
    <% end %>
  </div>
</div>